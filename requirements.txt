# Polymarket Official Client
# Per Grok Round 18: Pin to known stable version (0.36 doesn't exist yet)
# Test against latest before upgrading - order types may change
py-clob-client==0.34.1

# Async HTTP
aiohttp>=3.9.0
httpx>=0.25.0

# Web3 for on-chain claims
web3>=6.0.0

# Ethereum account management
eth-account>=0.10.0

# Environment variables
python-dotenv>=1.0.0

# HTTP requests for webhooks
requests>=2.31.0

# WebSocket for real-time orderbook updates
# Per Grok Round 7: Pin upper bound for reproducible builds
websockets>=12.0,<14.0
